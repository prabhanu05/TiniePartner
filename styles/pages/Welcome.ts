import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    smallContainer: {
        flex: 0.2,
        padding: 14,
        alignItems: 'center',
    },
    bigContainer: {
        flex: 0.8,
        padding: 14,
        alignItems: 'center',
    },
});

export default styles;
